%!TEX root = ../Thesis.tex

\definecolor{bordergray}{rgb}{0.4,0.4,0.4}
\definecolor{Kblue}{rgb}{0,0,0.7}
\definecolor{Kgreen}{rgb}{0,0.4,0}
\definecolor{Dark}{gray}{.2}
\definecolor{Medium}{gray}{.6}
\definecolor{Light}{gray}{.8}
\definecolor{DTUred}{RGB}{193,39,54}


%%%% Styling from Lasse and Kenneth

%%% Definition of headers and footers
% page style (header and footer)

\pagestyle{ruled}
%\pagestyle{plain} % centered page number and no header


\copypagestyle{source}{ruled}
\newcommand{\srcSec}{}
\makeevenhead{source}{\leftmark}{}{\srcSec}
\makeoddhead{source}{}{}{\thesection~--~\srcSec}


\makeatletter
\newif\ifNoChapNumber 
\makechapterstyle{phdt}{%
  \chapterstyle{default}
  \renewcommand{\printchaptername}{}
  \renewcommand{\chapternamenum}{}
  \renewcommand{\chaptitlefont}{\normalfont\Huge\bfseries}
  \renewcommand{\chapnumfont}{\normalfont\Huge\bfseries}
  \renewcommand{\printchapternum}{}%\chapnumfont \thechapter\space}
  \renewcommand{\afterchapternum}{}
  \renewcommand\printchapternonum{\NoChapNumbertrue} 
  \renewcommand*{\printchaptertitle}[1]{%
\ifNoChapNumber\relax\else%
    \@hangfrom{%
        \chapnumfont%
        \hspace{0.5em}%
        {\color{dtured}\thechapter}%
        \hspace{0.3em}%
        \begin{picture}(0,0)%
            \setlength\unitlength{1mm}%
            % The line of the bar
            \linethickness{1pt}%
            % put setc coordinate for start
            % line is \line(deltaX,deltaY){length}
            \ifthenelse{\thechapter > -1}{%
                \color{black}\put(0,-8.5){\line(0,1){28}}%
            }{%
                \color{black}\put(0,-2.5){\line(0,1){22}}%
            }%
        \end{picture}%
        \hspace{0.3em}%
    }%
\fi
       {\raggedright\chaptitlefont{##1}%
        \ifNoChapNumber%
        \vspace{-1cm}%
        \fi}
  }
}
\chapterstyle{phdt}
\makeatother

\setlength{\beforechapskip}{10pt}

\usepackage{ifthen}

% Counter is incremented before printing it. We want to start with 0, so set to -1.
%\setcounter{part}{-1}
\makeatletter
%\renewcommand*{\thepart}{\arabic{part}}
\renewcommand{\midpartskip}{
    \par\parbox{2.7cm}{\leavevmode\leaders\hrule height 1pt\hfill\kern\z@}\par
}
\renewcommand{\beforepartskip}{\vspace*{\fill}}
\renewcommand{\afterpartskip}{\vspace*{\fill}}
\makeatother


\begin{comment}

    
    
\renewcommand{\printpartname}{\partnamefont Act}
\renewcommand*{\printpartnum}{
    \ifthenelse{%
        \equal{\thepart}{I}
        }{0}
        {\ifthenelse{
            \equal{\thepart}{II}
            }{1 -- 4}
            {5 -- 7}}}
\end{comment}
